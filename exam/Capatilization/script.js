
// using ternary operator
function check(user) {
    let a ,UserFinal
    (UserFinal===user)? console.log(user):
    a=user[0].toUpperCase()
    UserFinal = a+user.slice(1)
    console.log(UserFinal)
}
check("chandraker")
